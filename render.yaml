services:
  - type: web
    name: facemorph
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: XAI_API_KEY
        sync: false
      - key: NODE_VERSION
        value: "20"
    healthCheckPath: /api/health
